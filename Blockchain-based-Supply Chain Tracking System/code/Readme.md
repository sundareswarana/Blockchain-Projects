The code index.html, style.js, app.js, contract.js and SupplyChain.sol were defined for the following tasks.

The user fills out the HTML form.

MetaMask prompts for transaction approval.

Ganache mines the transaction.

The smart contract stores the data.

The webpage retrieves and displays the stored product.
